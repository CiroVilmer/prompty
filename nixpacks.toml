providers = ["python"]

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["python -m pip install -r apps/api/requirements.txt"]

[start]
cmd = "python -m uvicorn apps.api.main:app --host 0.0.0.0 --port $PORT"
